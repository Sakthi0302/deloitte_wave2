Connected to:
Oracle Database 11g Enterprise Edition Release 11.2.0.1.0 - 64bit Production
With the Partitioning, OLAP, Data Mining and Real Application Testing options

SQL> create user DELOITTE IDENTIFIED BY deloitte;

User created.

SQL> grant dba to DELOITTE;

Grant succeeded.

SQL> CONNECT DELOITTE/deloitte;
Connected.
SQL> alter user DELOITTE identified by Deloitte;

User altered.

SQL> connect DELOITTE/Deloitte;
Connected.
SQL> select * from Worker;

no rows selected





1)
SELECT FIRST_NAME AS WORKER_NAME
FROM WORKER;

2)
SELECT UPPER(FIRST_NAME)
FROM WORKER;

3)
SELECT DISTINCT(DEPARTMENT)
FROM WORKER;

4)
SELECT INSTR(FIRST_NAME,'A')
FROM WORKER;

5)
SELECT SUBSTR(FIRST_NAME,1,3)
FROM WORKER;

6)
SELECT RTRIM(FIRST_NAME)
FROM WORKER;

7)
SELECT LTRIM(FIRST_NAME)
FROM WORKER;

8)
SELECT DISTINCT(DEPARTMENT),length(DEPARTMENT)
FROM WORKER;

9)
SELECT REPLACE(FIRST_NAME,'A','a')
FROM WORKER;

10)
SELECT (FIRST_NAME||' '||LAST_NAME) AS COMPLETE_NAME
FROM WORKER;

11)
SELECT * 
FROM WORKER
ORDER BY FIRST_NAME ASC;

12)
SELECT * 
FROM WORKER
ORDER BY FIRST_NAME ASC , DEPARTMENT DESC;

13)
SELECT * 
FROM WORKER
WHERE FIRST_NAME IN ('Vipul' , 'Satish');

14)
SELECT * 
FROM WORKER
WHERE FIRST_NAME NOT IN ('Vipul' , 'Satish');

15)
SELECT *
FROM WORKER
WHERE DEPARTMENT ='Admin';

16)
SELECT *
FROM WORKER
WHERE FIRST_NAME LIKE '%A%';

17)
SELECT *
FROM WORKER
WHERE FIRST_NAME LIKE '%A';

18)
SELECT *
FROM WORKER
WHERE FIRST_NAME LIKE '%H'
AND LENGTH(FIRST_NAME) = 6;

19)
SET SERVEROUTPUT ON;
DECLARE
V_DEPT VARCHAR(20);
BEGIN
 SELECT MAX(DEPARTMENT_ID) INTO V_DEPT FROM DEPARTMENTS;
 DBMS_OUTPUT.PUT_LINE('THE MAXIMUM DEPARTMENT ID IS '|| V_DEPT);
END; 

20)
SET SERVEROUTPUT ON;
DECLARE
"INSERT" VARCHAR(20);
BEGIN
 SELECT MAX(DEPARTMENT_ID) INTO "INSERT" FROM DEPARTMENTS;
 DBMS_OUTPUT.PUT_LINE('THE MAXIMUM DEPARTMENT ID IS '|| "INSERT");
END; 

21)
DECLARE
 V_NAME VARCHAR(20) := 'SPORT';
 V_ID NUMBER(20) :=001;
BEGIN
 DECLARE
  V_NAME VARCHAR(20) := 'UNISPORT';
  V_ID NUMBER(20) := 002;
 BEGIN
  DBMS_OUTPUT.PUT_LINE('NAME = '||V_NAME);
  DBMS_OUTPUT.PUT_LINE('ID = '||V_ID);
END;
  DBMS_OUTPUT.PUT_LINE('NAME = '||V_NAME);
  DBMS_OUTPUT.PUT_LINE('ID = '||V_ID);
END;

22)
DECLARE
 V_SAL NUMBER(20);
BEGIN
 SELECT SALARY+1000 INTO V_SAL FROM EMPLOYEES WHERE EMPLOYEE_ID =122;
 DBMS_OUTPUT.PUT_LINE('UPDATED SALARY IS : ' ||V_SAL);
END;
