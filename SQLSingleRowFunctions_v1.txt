1) SELECT CUSTOMER_ID,LOWER(FIRST_NAME),UPPER(LAST_NAME)
FROM CUSTOMERS
WHERE CUSTOMER_ID BETWEEN 80 AND 150;

2) SELECT LAST_NAME,FIRST_NAME,(SUBSTR(FIRST_NAME,1,1)||SUBSTR(LAST_NAME,1,3)||'@MYMAIL.COM')
FROM CUSTOMERS;
   SELECT LAST_NAME,FIRST_NAME,(SUBSTR(FIRST_NAME,1,1)||SUBSTR(LAST_NAME,LENGTH(LAST_NAME)-2,3)||'@MYMAIL.COM')
FROM CUSTOMERS;

3)SELECT LAST_NAME,LENGTH(LAST_NAME)
FROM CUSTOMERS
WHERE LENGTH(LAST_NAME) > 9 ;

4) SELECT FIRST_NAME,LAST_NAME,MAIN_PHONE_NUM,REPLACE(MAIN_PHONE_NUM,515,'$$$') AS NEW_PHONE_NUM
FROM CUSTOMERS;
   SELECT FIRST_NAME,LAST_NAME,MAIN_PHONE_NUM,(REPLACE(SUBSTR(MAIN_PHONE_NUM,1,3),515,'$$$')||SUBSTR(MAIN_PHONE_NUM,4)) AS NEW_PHONE_NUM
FROM CUSTOMERS;

5) SELECT FIRST_NAME 
FROM CUSTOMERS;
   SELECT MONTHLY_DISCOUNT
FROM CUSTOMERS;
    SELECT MONTHLY_DISCOUNT+19.7/100*MONTHLY_DISCOUNT
FROM CUSTOMERS;

    SELECT ROUND(MONTHLY_DISCOUNT+19.7/100*MONTHLY_DISCOUNT)
FROM CUSTOMERS;

    SELECT FLOOR(MONTHLY_DISCOUNT+19.7/100*MONTHLY_DISCOUNT)
FROM CUSTOMERS;
    SELECT CEIL(MONTHLY_DISCOUNT+19.7/100*MONTHLY_DISCOUNT)
FROM CUSTOMERS;

6) SELECT FIRST_NAME,JOIN_DATE,JOIN_DATE-10,ADD_MONTHS(JOIN_DATE,1),TRUNC(SYSDATE-JOIN_DATE)
FROM CUSTOMERS;
   SELECT FIRST_NAME,BIRTH_DATE,TRUNC(MONTHS_BETWEEN(SYSDATE,BIRTH_DATE)/12) AS AGE
FROM CUSTOMERS
WHERE TRUNC(MONTHS_BETWEEN(SYSDATE,BIRTH_DATE)/12) > 50;
   SELECT * 
FROM CUSTOMERS 
WHERE EXTRACT(DAY,SYSDATE)=EXTRACT(DAY,BIRTH_DATE) AND EXTRACT(MONTH,SYSDATE)=EXTRACT(MONTH,BIRTH_DATE);
   SELECT FIRST_NAME,JOIN_DATE,TRUNC(MONTHS_BETWEEN(SYSDATE,JOIN_DATE)/12)
FROM CUSTOMERS
WHERE TRUNC(MONTHS_BETWEEN(SYSDATE,JOIN_DATE)/12) =5; 